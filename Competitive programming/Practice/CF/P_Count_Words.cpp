#include <bits/stdc++.h>
using namespace std;

long long GCD(long long a, long long b)
{
    if (a == 0)
        return b;
    else
        return gcd(b % a, a);
}

void solve()
{
    stringstream
}

long long main()
{
    // long long test;
    // cin >> test;
    // while(test--){
    solve();
    // }
}